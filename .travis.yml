language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t pscheminger/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pscheminger/docker-react npm run test
